{{ form_start(form) }}
    {{ form_row(form.password, {
        label: 'Mot de passe'
    }) }}
{{form_rest(form)}}
    <button type="submit" class="btn btn-primary mt-4">Enregistrer</button>
{{form_end(form)}}